import { RegexpCleanerPipe } from './regexp.pipe';

describe('RegexpCleanerPipe', () => {
  it('create an instance', () => {
    const pipe = new RegexpCleanerPipe();
    expect(pipe).toBeTruthy();
  });
});
