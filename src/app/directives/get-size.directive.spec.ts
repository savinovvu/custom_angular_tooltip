import { DrawWindowDirective } from './get-size.directive';

describe('DrawWindowDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawWindowDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
